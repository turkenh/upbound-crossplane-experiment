apiVersion: apiextensions.crossplane.io/v1alpha1
kind: Usage
metadata:
  name: using-uses-used
spec:
  of:
    apiVersion: nop.crossplane.io/v1alpha1
    kind: NopResource
    resourceSelector:
        matchLabels:
            foo: bar
  reason: "Testing usage of a used resource"
